package com.labforward.api.core.validation;

import javax.validation.groups.Default;

public interface EntityUpdateValidatorGroup extends Default {

	// do nothing - validation group marker interface
}
